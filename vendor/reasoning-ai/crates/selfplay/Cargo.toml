[package]
name = "reasoning-selfplay"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Reasoning AI — selfplay crate (Rust port of the Python reasoning-ai project)"

[dependencies]
reasoning-common = { path = "../common" }
reasoning-search = { path = "../search" }
reasoning-prm = { path = "../prm" }
rand = { workspace = true }
