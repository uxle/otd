[package]
name = "reasoning-common"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Reasoning AI — shared helpers (Python float semantics, exact rationals, rational matrices)"

[dependencies]
