[package]
name = "reasoning-tokenizer"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Reasoning AI — tokenizer crate (Rust port of the Python reasoning-ai project)"

[dependencies]
