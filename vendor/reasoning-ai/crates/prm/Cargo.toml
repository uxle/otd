[package]
name = "reasoning-prm"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Reasoning AI — prm crate (Rust port of the Python reasoning-ai project)"

[dependencies]
reasoning-common = { path = "../common" }
reasoning-verifier = { path = "../verifier" }
reasoning-search = { path = "../search" }
reasoning-symbolic = { path = "../symbolic" }
rand = { workspace = true }
