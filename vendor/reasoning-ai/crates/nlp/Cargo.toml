[package]
name = "reasoning-nlp"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Reasoning AI — NL quantity extraction and deterministic routing (parser + math routes)"

[dependencies]
reasoning-common = { path = "../common" }
reasoning-science = { path = "../science" }
reasoning-curriculum = { path = "../curriculum" }
regex = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
