[package]
name = "reasoning-rl"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Reasoning AI — RL training stack (PPO/GRPO math, policies, training loops)"

[dependencies]
reasoning-common = { path = "../common" }
reasoning-search = { path = "../search" }
reasoning-prm = { path = "../prm" }
rand = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }

# NOTE: `reasoning-curriculum` is intentionally NOT a dependency yet.
# rl uses it only in `curriculum_grpo.rs` (CurriculumController from
# curriculum::progression); that crate is being ported by another agent.
# See src/curriculum_grpo.rs for the guarded stub.
