[package]
name = "reasoning-neural"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Reasoning AI — neural crate (Rust port of the Python reasoning-ai project)"

[dependencies]
reasoning-tokenizer = { path = "../tokenizer" }
reasoning-search = { path = "../search" }
reasoning-symbolic = { path = "../symbolic" }
rand = { workspace = true }
